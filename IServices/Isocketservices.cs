using adapter.Model.socketmodel;

namespace adapter.IServices
{
    public interface Isocketservices
    {
        //Task<IEnumerable<T>> GetAll();
        //Task<IEnumerable<socketmodel>> InstallmentWiseFeeBalanceList(socketmodel Entity);
    }
}
